<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Render</title>
    <link rel="stylesheet" href="/assets/css/index.css">
</head>

<body>
    <div id="card" class="flex col gap-16">
        <div id="top" class="flex align-center gap-16">
            <div id="logo" class="flex-grow flex-no-shrink">
                <span class="white">osu!</span><span class="green">complete</span>
            </div>
            <div id="category"><%= categoryName %></div>
        </div>
        <%- include('../../partials/userCard', {user, noFade: 'noFade'}) %>
        <div id="stats" class="flex col gap-8 justify-center align-center flex-grow">
            <div class="flex gap-32">
                <div class="stat primary">
                    <div class="name">rank</div>
                    <div class="value">#<%= stats.rank == -1 ? '-' : stats.rank %></div>
                </div>
                <div class="stat primary">
                    <div class="name">completion xp</div>
                    <div class="value"><%= stats.xp.toLocaleString() %></div>
                </div>
            </div>
            <div class="flex gap-24">
                <div class="stat">
                    <div class="name">% completed</div>
                    <div class="value"><%= stats.percentage_completed.toFixed(2) %>%</div>
                </div>
                <div class="stat">
                    <div class="name">maps completed</div>
                    <div class="value"><%= stats.count_completed.toLocaleString() %></div>
                </div>
                <div class="stat">
                    <div class="name">total maps</div>
                    <div class="value"><%= stats.count_total.toLocaleString() %></div>
                </div>
            </div>
        </div>
        <div id="bar">
            <div class="filling" style="width: <%= stats.percentage_completed %>%; background: <%= percentageColor %>">
            </div>
        </div>
    </div>
</body>

<style>
    #card {
        position: fixed;
        top: 0px;
        left: 0px;
        width: 100%;
        height: 100%;
        padding: 16px;
        background: hsl(220, 20%, 15%);
    }

    #logo {
        font-size: 20px;
        font-weight: 600;
    }

    #category {
        font-size: 16px;
        line-height: 1.2;
        text-align: right;
    }

    #stats .stat {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 2px;
    }

    #stats .stat .name {
        color: var(--textDim);
        font-size: 18px;
        font-weight: 600
    }

    #stats .stat.primary .name {
        font-size: 20px;
    }

    #stats .stat .value {
        color: hsl(220, 20%, 80%);
        font-size: 28px;
        font-weight: 400;
    }

    #stats .stat.primary .value {
        color: white;
        font-weight: 600;
        font-size: 36px;
    }

    #bar {
        width: 100%;
        height: 8px;
        border-radius: 8px;
        background: hsl(220, 20%, 10%);
    }

    #bar .filling {
        height: 100%;
        border-radius: 8px;
    }
</style>

</html>